import{_ as t,o as s,c as a,a0 as i}from"./chunks/framework.iBgbCoJu.js";const k=JSON.parse('{"title":"Migration — camelCase fields","description":"","frontmatter":{},"headers":[],"relativePath":"guide/development/migration-camelcase.md","filePath":"guide/development/migration-camelcase.md"}'),d={name:"guide/development/migration-camelcase.md"};function o(n,e,r,c,l,h){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="migration-—-camelcase-fields" tabindex="-1">Migration — camelCase fields <a class="header-anchor" href="#migration-—-camelcase-fields" aria-label="Permalink to &quot;Migration — camelCase fields&quot;">​</a></h1><p>Hub&#39;s HTTP API, domain types and npm packages moved every field name from <code>snake_case</code> to <code>camelCase</code>. Database column names did <strong>not</strong> change.</p><p>This is a breaking change. There are <strong>no</strong> <code>snake_case</code> aliases and no deprecation window — and, importantly, <strong>almost nothing errors</strong>: an old name is silently dropped by the query parser or silently ignored by the validator, depending on where it appears. Do not expect a 4xx to find these for you.</p><p>Tracks <a href="https://github.com/PrivateAIM/hub/issues/1501" target="_blank" rel="noreferrer">#1501</a>.</p><h2 id="what-changed" tabindex="-1">What changed <a class="header-anchor" href="#what-changed" aria-label="Permalink to &quot;What changed&quot;">​</a></h2><p><code>realm_id</code> → <code>realmId</code>, <code>created_at</code> → <code>createdAt</code>, <code>display_name</code> → <code>displayName</code>, and so on for every multi-word field. The rename applies uniformly to:</p><ul><li><strong>Response bodies</strong> — every entity field, in both the <code>{ data, meta }</code> record envelope and collections.</li><li><strong>Request bodies</strong> — <code>POST</code> / <code>PATCH</code> payloads for every entity.</li><li><strong>Query parameters</strong> — the rapiq vocabulary: <code>fields</code>, <code>filter</code>, <code>sort</code>, <code>include</code>. Relation names move too (<code>include=master_image</code> → <code>include=masterImage</code>).</li><li><strong><code>meta.schema</code></strong> — the published allow-lists now list camelCase keys.</li><li><strong>Telemetry log labels</strong> — <code>ref_type</code> / <code>ref_id</code> → <code>refType</code> / <code>refId</code> (<code>LogFlag</code>), and alongside them <code>actor_type</code> / <code>actor_id</code> → <code>actorType</code> / <code>actorId</code>, <code>target_type</code> / <code>target_id</code> → <code>targetType</code> / <code>targetId</code>, and <code>bucket_type</code> → <code>bucketType</code>.</li><li><strong>The npm packages</strong> — <code>@privateaim/core-kit</code>, <code>core-http-kit</code>, <code>storage-kit</code>, <code>telemetry-kit</code>, <code>messenger-kit</code>, <code>messenger-http-kit</code>, <code>client-vue</code>.</li></ul><h3 id="before-after" tabindex="-1">Before / after <a class="header-anchor" href="#before-after" aria-label="Permalink to &quot;Before / after&quot;">​</a></h3><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /nodes?filter[realm_id]=&lt;id&gt;&amp;sort=-created_at&amp;fields=%2Bexternal_name&amp;include=registry_project</span></span></code></pre></div><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /nodes?filter[realmId]=&lt;id&gt;&amp;sort=-createdAt&amp;fields=%2BexternalName&amp;include=registryProject</span></span></code></pre></div><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// before</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;display_name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Node A&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;realm_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;created_at&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;meta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {} }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// after</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;displayName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Node A&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;realmId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;createdAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;meta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {} }</span></span></code></pre></div><h2 id="what-did-not-change" tabindex="-1">What did NOT change <a class="header-anchor" href="#what-did-not-change" aria-label="Permalink to &quot;What did NOT change&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Surface</th><th>Still <code>snake_case</code></th></tr></thead><tbody><tr><td>Database columns</td><td><code>realm_id</code>, <code>created_at</code>, … — pinned per column, unchanged</td></tr><tr><td>Permission names</td><td><code>analysis_create</code>, <code>node_update</code>, <code>registry_manage</code>, …</td></tr><tr><td>OAuth2 / OIDC parameters</td><td><code>client_id</code>, <code>client_secret</code>, <code>grant_type</code>, <code>redirect_uri</code>, <code>code_verifier</code></td></tr><tr><td>Token introspection payloads</td><td><code>realm_id</code>, <code>sub_kind</code>, <code>sub_name</code>, …</td></tr><tr><td>Environment variables</td><td><code>DB_TYPE</code>, <code>AUTHUP_URL</code>, <code>MINIO_*</code>, …</td></tr><tr><td>Table names</td><td><code>analysis_nodes</code>, <code>registry_projects</code>, …</td></tr></tbody></table><p>Permission names are unchanged because they are stored in Authup, so nothing has to be re-provisioned.</p><h2 id="upgrading" tabindex="-1">Upgrading <a class="header-anchor" href="#upgrading" aria-label="Permalink to &quot;Upgrading&quot;">​</a></h2><h3 id="api-and-npm-consumers" tabindex="-1">API and npm consumers <a class="header-anchor" href="#api-and-npm-consumers" aria-label="Permalink to &quot;API and npm consumers&quot;">​</a></h3><p>Rename the fields you send and read. <code>meta.schema</code> on any query-capable <code>GET</code> is the authoritative list of accepted <code>fields</code> / <code>filter</code> / <code>sort</code> / <code>include</code> keys for that endpoint — query it if you are unsure:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /nodes?pagination[limit]=1</span></span></code></pre></div><p>Failure modes to expect while migrating:</p><ul><li>An unknown <strong>filter</strong>, <strong>sort</strong> or <strong><code>include</code></strong> key is <strong>silently dropped, not rejected</strong>. rapiq&#39;s <code>throwOnFailure</code> is deliberately not enabled, so a stale <code>filter[realm_id]</code> does not fail — the filter is pruned and the endpoint answers with a <strong>wider, unfiltered</strong> result set. This is the failure mode to watch for: it looks like success. (<code>strict: true</code> on hub&#39;s schemas does not change this — it governs parameters that declare <em>no</em> allow-list, and every hub schema declares one.)</li><li>An unknown <strong>request-body</strong> key is dropped by the validator, so a write appears to succeed while leaving the field unset. Check the response body.</li><li>An unknown <strong><code>fields</code></strong> entry does not error; the field is simply absent from the response.</li></ul><p>Because none of these raise, diff every key you send against <code>meta.schema</code> rather than waiting for an error.</p><h3 id="database" tabindex="-1">Database <a class="header-anchor" href="#database" aria-label="Permalink to &quot;Database&quot;">​</a></h3><p>No action. No migration ships with this change: every renamed property carries an explicit column name, so the physical schema is byte-identical. A run → revert → run round-trip over the existing migrations is unaffected.</p><h3 id="telemetry-logs" tabindex="-1">Telemetry / logs <a class="header-anchor" href="#telemetry-logs" aria-label="Permalink to &quot;Telemetry / logs&quot;">​</a></h3><p>Log lines written <strong>before</strong> the upgrade carry <code>ref_type</code> / <code>ref_id</code> labels and are not selectable under the new <code>refType</code> / <code>refId</code> names — VictoriaLogs stores label keys verbatim. Entity views (the analysis log panel) will therefore show only post-upgrade lines. Existing rows are not rewritten; they age out with your retention policy. Query old lines directly by their old label names if you need them.</p><p>Anything outside Hub that writes logs (node-side components posting to <code>/logs</code> or <code>/analysis-node-logs</code>) must switch label names in lockstep.</p><h3 id="node-side-components" tabindex="-1">Node-side components <a class="header-anchor" href="#node-side-components" aria-label="Permalink to &quot;Node-side components&quot;">​</a></h3><p>These flat, non-envelope endpoints changed field names and need coordinated node updates:</p><ul><li><code>POST /analysis-node-logs</code> — body keys <code>analysis_id</code>, <code>node_id</code>, <code>node_realm_id</code>, <code>analysis_realm_id</code> → camelCase.</li><li><code>GET</code> / <code>POST /nodes/:id/client/credentials</code> — <code>display_name</code> → <code>displayName</code>, in the response <strong>and</strong> in the <code>POST</code> request body. The body has no validator, so a legacy <code>display_name</code> key is silently dropped: the secret still rotates and the response is <code>200</code>, while the display name stays unchanged.</li><li><code>GET</code> / <code>POST /analyses/:id/client/credentials</code> — response <code>display_name</code> → <code>displayName</code>. The <code>POST</code> body (<code>{ secret }</code>) is unchanged. Node-callable: a client whose node holds an approved <code>analysisNode</code> row may read these.</li><li><code>GET /nodes/:id/registry/credentials</code> — <code>account_name</code>, <code>account_secret</code>, <code>external_name</code> → camelCase.</li><li><code>POST /analyses/:id/client/permissions</code> — body key <code>permission_id</code> → <code>permissionId</code>.</li><li>The messenger broker surface (<code>POST /messages</code>, pull, ack) — <code>sender_type</code>, <code>sender_id</code>, <code>recipient_type</code>, <code>recipient_id</code>, <code>created_at</code> → camelCase.</li></ul><h3 id="stored-urls" tabindex="-1">Stored URLs <a class="header-anchor" href="#stored-urls" aria-label="Permalink to &quot;Stored URLs&quot;">​</a></h3><p>Bookmarked or emailed links carrying <code>?filter[realm_id]=…</code>-style query strings stop filtering as intended: the key is silently dropped, so the link returns an unfiltered result set rather than an error. Re-create them.</p><h3 id="audit-event-diffs" tabindex="-1">Audit event diffs <a class="header-anchor" href="#audit-event-diffs" aria-label="Permalink to &quot;Audit event diffs&quot;">​</a></h3><p>Telemetry <code>events.data.diff</code> records which entity fields changed, keyed by field name. Rows written before the upgrade keep their <code>snake_case</code> keys. They are rendered as-is and never matched by name, so they are left untouched rather than rewritten.</p><h3 id="authup-attribute-policies" tabindex="-1">Authup attribute policies <a class="header-anchor" href="#authup-attribute-policies" aria-label="Permalink to &quot;Authup attribute policies&quot;">​</a></h3><div class="warning custom-block"><p class="custom-block-title">Read this before enabling attribute policies</p><p>Nothing here changes behaviour today, and there is nothing to do at upgrade time. It matters the first time an <strong>attribute policy</strong> is switched on in Authup.</p></div><p>Five server-core permission checks hand a whole <code>Analysis</code> entity to <code>@authup/access</code> as an attribute bag:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> actor.permissionChecker.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">check</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    name: PermissionName.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ANALYSIS_UPDATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    data: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PolicyData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ [BuiltInPolicyType.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ATTRIBUTES</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]: entity }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><ul><li><code>apps/server-core/src/core/entities/analysis/service.ts</code> — create, update, delete</li><li><code>apps/server-core/src/core/services/client-credential/analysis.ts</code></li><li><code>apps/server-core/src/app/modules/database/analysis-client-permission.ts</code></li></ul><p>The attribute <strong>policies</strong> that match on those key names live in <strong>Authup&#39;s database</strong>, not in this repository, so the rename could not carry them along. As hub is wired today the bag is never read: token introspection delivers policy-free grants, so the evaluator never receives a <code>policy</code> field and every verdict is <code>allow</code>. The mismatch is therefore latent.</p><p>It becomes real the moment an attribute policy is attached to an analysis permission. Any policy still written against the old key names stops matching:</p><table tabindex="0"><thead><tr><th>Old attribute key</th><th>New attribute key</th></tr></thead><tbody><tr><td><code>realm_id</code></td><td><code>realmId</code></td></tr><tr><td><code>user_id</code></td><td><code>userId</code></td></tr><tr><td><code>project_id</code></td><td><code>projectId</code></td></tr><tr><td><code>master_image_id</code></td><td><code>masterImageId</code></td></tr><tr><td><code>registry_id</code></td><td><code>registryId</code></td></tr><tr><td><code>client_id</code></td><td><code>clientId</code></td></tr><tr><td><code>display_name</code></td><td><code>displayName</code></td></tr><tr><td><code>configuration_locked</code></td><td><code>configurationLocked</code></td></tr><tr><td><code>configuration_*_valid</code></td><td><code>configuration*Valid</code></td></tr><tr><td><code>build_status</code>, <code>build_os</code>, <code>build_hash</code>, …</td><td><code>buildStatus</code>, <code>buildOs</code>, <code>buildHash</code>, …</td></tr><tr><td><code>distribution_status</code>, <code>distribution_progress</code></td><td><code>distributionStatus</code>, <code>distributionProgress</code></td></tr><tr><td><code>execution_status</code>, <code>execution_progress</code></td><td><code>executionStatus</code>, <code>executionProgress</code></td></tr><tr><td><code>nodes_approved</code></td><td><code>nodesApproved</code></td></tr><tr><td><code>created_at</code>, <code>updated_at</code></td><td><code>createdAt</code>, <code>updatedAt</code></td></tr></tbody></table><p><strong>A policy that no longer matches fails in the direction the policy was shaped for.</strong> An allowlist-shaped policy (<code>realm_id == X</code> ⇒ allow) stops granting and fails closed — noisy, but safe. A <strong>denylist</strong>-shaped policy (<code>configuration_locked == true</code> ⇒ deny) stops denying and fails <strong>open</strong>, which is silent. Audit any deployed attribute policy against the table above before enabling it.</p>`,42)])])}const g=t(d,[["render",o]]);export{k as __pageData,g as default};
